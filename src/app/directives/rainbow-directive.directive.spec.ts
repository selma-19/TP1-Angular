import { RainbowDirective} from './rainbow-directive.directive';

describe('RainbowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowDirective();
    expect(directive).toBeTruthy();
  });
});
